###############################################################################
# SWD-DZ-4 — robust .gitattributes for .NET 9 + Rider on macOS
# Goals:
#  - stable line endings across OS (LF in repo)
#  - avoid noisy diffs for binaries
#  - sane diff/merge behavior for common file types
###############################################################################

# ---------------------------
# Default text normalization
# ---------------------------
* text=auto eol=lf

# ---------------------------
# Explicit text files (force LF)
# ---------------------------
*.cs          text eol=lf
*.csproj      text eol=lf
*.props       text eol=lf
*.targets     text eol=lf
*.sln         text eol=lf
*.json        text eol=lf
*.yml         text eol=lf
*.yaml        text eol=lf
*.xml         text eol=lf
*.config      text eol=lf
*.md          text eol=lf
*.txt         text eol=lf
*.sh          text eol=lf
Dockerfile    text eol=lf
*.dockerignore text eol=lf
*.gitignore   text eol=lf
*.gitattributes text eol=lf

# ---------------------------
# Treat these as binary (no EOL conversion, no diff noise)
# ---------------------------
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.pdf   binary
*.zip   binary
*.gz    binary
*.tgz   binary
*.7z    binary
*.rar   binary
*.tar   binary
*.exe   binary
*.dll   binary
*.pdb   binary
*.so    binary
*.dylib binary
*.nupkg binary

# Certificates / keys are binary (and should also be gitignored)
*.pfx   binary
*.snk   binary
*.pem   binary
*.key   binary
*.crt   binary
*.cer   binary

# ---------------------------
# Better diffs for specific formats
# ---------------------------
*.csproj diff=xml
*.props  diff=xml
*.targets diff=xml
*.xml    diff=xml

# ---------------------------
# Merge strategy hints (optional but useful)
# Prefer keeping "ours" on lock files to reduce conflicts.
# Uncomment if you add these files to the repo.
# ---------------------------
# package-lock.json merge=ours
# yarn.lock         merge=ours
